import React from 'react';

type CollectionTreeProps = {};

const CollectionTree = (props: any) => {
  const { collections } = props;
  return <div>CollectionTree</div>;
};

export default CollectionTree;
